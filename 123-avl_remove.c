#include "binary_trees.h"

/**
 * avl_remove - removes a node from an AVL tree
 *		once located, the node must be removed & freed
 *		If the node to be deleted has two children,
 *		must be replaced with its first in-order successor (not predecessor)
 *		after deletion, the tree must be balanced if necessary
 * @root: pointer to the root node of the tree for removing the node
 * @value: value to remove in the tree
 * Return: pointer to the new root node
 *		of the tree after removing value & rebalancing
 */
avl_t *avl_remove(avl_t *root, int value)
{

}
